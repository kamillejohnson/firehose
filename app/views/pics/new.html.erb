<div class="span5 text-center hottub-layer">
	<h2>
		whats good
		<%= simple_form_for @pic do |f| %>
		<%= f.input :hour %>
		<%= f.input :emotion %>
		<%= f.input :learned %>
		<%= f.submit 'Save' %>
		<% end %>
	</h2>
</div>